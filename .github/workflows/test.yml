name: Unit Tests

on:
  pull_request:
    types: [ opened, synchronize ]
    paths-ignore:
      - '.env.*'
      - '**/*.md'
      - '**/*.sh'
      - '**/*.yml'
      - 'fastlane/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run_tests:
    name: Run tests
    runs-on: macos-14
    timeout-minutes: 15
    steps:
      - uses: passepartoutvpn/action-access-private-repositories@master
        with:
          access_token: ${{ secrets.ACCESS_TOKEN }}
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: 15.4
      - name: Run tests
        run: |
          cd Passepartout/Library
          swift test
